import React from 'react'
import { shallow } from 'enzyme'
import AlarmCalculator from '../src/settings/AlarmCalculator'


describe('alarmcalc component', () => {
  it('should render without errors', () => {
    expect(1).toBe(2);
  })
})
